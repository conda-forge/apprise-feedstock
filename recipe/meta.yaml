{% set name = "apprise" %}
{% set version = "1.3.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/apprise-{{ version }}.tar.gz
  sha256: 2c50c19a5dd41317b1f659c52b21a990febe6c15e08464228a1ce8e6098f11bf

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv
  number: 0

requirements:
  host:
    - babel
    - pip
    - python >=3.6
  run:
    - click >=5.0
    - markdown
    - python >=3.6
    - pyyaml
    - requests
    - requests-oauthlib
    - six

test:
  imports:
    - apprise
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/caronc/apprise
  summary: Push Notifications that work with just about every platform
  license: MIT
  license_file:
    - LICENSE

extra:
  recipe-maintainers:
    - thewchan
